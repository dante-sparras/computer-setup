# Dotfiles

## Prerequisites

- [Chezmoi](https://chezmoi.io/)

# Set up a new machine

The following command will install all dotfiles on a new machine:

```bash
chezmoi init --apply "dante-sparras"
```